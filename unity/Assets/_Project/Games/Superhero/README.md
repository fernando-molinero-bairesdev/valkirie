# Superhero (game)

Game-specific content: `IncidentDefinition` assets, `SpawnZone`-populated city scenes,
hero/monster `EntityArchetype` + `CharacterDefinition` assets, and the Netcode for
GameObjects wiring (not yet part of this scaffold - `IncidentInstance` and its trigger
evaluation must run server-authoritative only; see the comment on that class).
