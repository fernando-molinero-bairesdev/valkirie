# Valkirie (game)

Game-specific content only: `EraDefinition` assets, level scenes, `ObjectiveSet` assets,
character/enemy `EntityArchetype` assets. No new Core code should be needed to build a
battlefield - if it turns out you do need new code here, that's a signal to check whether
it actually belongs in Core instead.
